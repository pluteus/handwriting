指で書いて、
そのままメモにする。
走り書きメモは、タッチ画面に指でひと文字ずつ書いて、手書き文字のまま並べてメモを作るウェブアプリです。インストール不要で、オフラインでも使えます。

手書き文字をそのまま「活字」のように並べる
画面の左右2つのキャンバスに交互に文字を書くと、書き上がった文字が罫線エリアに順番に並んでいきます。タイピングではなく、自分の手書き文字でメモを作る、まったく新しい入力体験です。

アプリはブラウザ上で動作し、インストールは不要。ホーム画面に追加することで、ネイティブアプリのようにオフラインでも起動できます。書いた内容は自動的に端末に保存されるので、途中で画面を閉じても続きから再開できます。

主な機能
手書き入力を中心に、快適なメモ作成のための機能を備えています。

✍️
2キャンバス交互入力
左右2枚のキャンバスに交互に文字を書くと自動で確定されます。書き終わった文字はすぐ次の入力に移れるため、テンポよく書き続けられます。

⏱️
自動確定タイマー
一定時間（デフォルト1秒）書く動作が止まると文字が自動確定されます。設定で秒数を変更でき、自分のペースに合わせて調整できます。

💾
自動ローカル保存
文字を確定するたびに内容が端末のローカルストレージへ自動保存されます。アプリを閉じても次回起動時に続きから再開できます。

↶
元に戻す／やり直し
最大100ステップの操作履歴を保持。誤って文字を削除してしまっても、ツールバーの「↶」ボタンですぐ取り消せます。

📋
選択・コピー・切り取り・貼り付け
文字をタップして選択し、アプリ内でコピー・切り取り・貼り付けができます。選択範囲のドラッグや全選択にも対応しています。

🔍
ピンチズームで拡大確認
罫線エリアをピンチ操作で拡大・縮小できます。細かい字や長文になっても、ズームして見やすい倍率で確認できます。

📤
画像として書き出し・共有
書いたメモを画像として保存したり、Google Keepなどのアプリへ直接共有できます。手書きのまま記録を残せます。

⛶
フルスクリーンモード
ブラウザのUIを隠してフルスクリーンで使用できます。より広い画面で入力エリアを最大限に活用できます。

📵
PWA対応
ホーム画面に追加するとネイティブアプリと同様に起動できます。

How to use

使い方
シンプルな操作でどなたでもすぐに使い始められます。

ブラウザで開く
スマートフォンやタブレットのブラウザでアプリのURLにアクセスします。インストールは不要です。

左のキャンバスに書く
画面下部の左側キャンバスに指で1文字書きます。なぞった筆跡がそのまま表示されます。

右に書くと自動確定
続けて右側キャンバスに次の文字を書くと、左の文字が確定されて罫線エリアに追加されます。

繰り返して文章を作る
左→右→左→右と交互に書き続けることで、手書き文字が横に並んで文章になっていきます。

ホーム画面に追加（任意）
ブラウザの「ホーム画面に追加」からPWAとしてインストールできます。

画像として保存・共有
ツールバーの💾ボタンでメモを画像保存、📤ボタンで他のアプリへ共有できます。

Environments

対応環境
モダンブラウザであれば端末を問わず動作します。

📱
Android
Chrome / Firefox など主要ブラウザで動作。PWAホーム画面追加に対応。

🍎
iPhone / iPad
Safari で動作。ホーム画面に追加でスタンドアロン起動が可能です。

💻
PC（タッチ対応）
タッチスクリーン搭載のWindowsやMacでも指・ペンで入力できます。

🖥️
PC（マウス）
マウスでも描画可能。キーボードのBackspace・Enter・矢印キーにも対応。

なぜ「走り書きメモ」を作ったのか
このアプリは「もっと気軽に、もっと素早く、手書きでメモを残したい」という思いから生まれました。 多くのメモアプリは多機能で便利な一方、起動が重かったり、アカウント登録が必要だったりと、 ちょっとしたメモを残すにはハードルが高い場面があります。 そこで、ブラウザだけで動作し、ページを開いた瞬間に書き始められる軽量な手書きメモツールを目指しました。

Technology

ブラウザだけで動く仕組み
描画には HTML5 Canvas と Pointer Events API を使用しています。 ペン・指・マウスの入力を統一的に扱えるため、デバイスを選ばず自然な書き心地を実現できます。 また、描いた内容はブラウザの LocalStorage に保存され、サーバーに送信されることはありません。 そのため、オフライン環境でも利用でき、プライバシー面でも安心して使えます。

パフォーマンス面では、ストロークの補間やバッファリングを行い、低スペック端末でも滑らかに動作するよう最適化しています。 これらの処理はすべてクライアント側で完結しており、インストール不要で軽快に動作するのが特徴です。

Design

書くことに集中できる UI
走り書きメモは「余計なものを置かない」ことを大切にしています。 Undo/Redo も直感的に操作できます。 UI を最小限に抑えることで、紙に書くような感覚でメモに集中できる体験を目指しました。

Roadmap

今後のアップデート予定
安定性・高速性の向上
ユーザーのフィードバックをもとに、より使いやすいツールへ継続的に改善していきます。

Author

作者について
Web 技術を中心に個人開発を行っています。 「ブラウザだけでどこまで快適な体験を作れるか」をテーマに、UI/UX の研究や小さなツールの開発を続けています。 このアプリもその実験の一つとして制作しました。
